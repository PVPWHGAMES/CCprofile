# 全局 Claude Code 指令

## 语言偏好

- 所有面向用户的输出、解释说明、注释均使用**中文**
- 代码中的注释使用中文
- Git commit message 使用中文
- 生成的文档使用中文

## 编码风格

- 优先使用项目已有的约定和模式
- 遵循项目的 Checkstyle 规则（如有）
- 修改代码前先了解现有架构，避免破坏一致性
- 输出注释时不需要写出顺序，直接写具体的操作

## 工作方式

- 重要修改前先说明计划，获得确认后再执行
- 遇到不确定的情况时主动询问
- 完成任务后简要总结改动内容
- 使用 Context7 查询最新的库/框架文档
- 安装软件/程序后，自动清理下载的压缩包、解压临时目录等临时文件

## Git 规范

- 不主动 commit，除非用户明确要求
- 不主动 push，除非用户明确要求
- 建议的 commit 格式：`<type>: <描述>`
  - feat: 新功能
  - fix: 修复
  - refactor: 重构
  - docs: 文档
  - style: 格式
  - test: 测试
  - chore: 构建/工具

## 配置同步

- 当用户提到「同步配置」、「备份配置」、「上传配置」、「恢复配置」等语句时，使用 `cc-config-sync` 处理
- 常用命令：
  - `cc-config-sync pull` — 将本地配置同步到仓库
  - `cc-config-sync push` — 将仓库配置同步到本地
  - `cc-config-sync status` — 查看本地与仓库的差异
  - `cc-config-sync list` — 列出所有跟踪的路径
- 仓库路径通过 `cc-config-sync config show` 查看
- 同步到仓库后需手动 `git add`、`git commit`、`git push` 推送到远程
- **注意**：`settings.json` 中的 `ANTHROPIC_AUTH_TOKEN` 和 `ANTHROPIC_BASE_URL` 是敏感信息，提交前需脱敏处理

## 安全意识

- 不在代码中硬编码密码、密钥等敏感信息
- 配置文件中的敏感信息需要提醒用户注意保护

## 安全审计（自动激活）

当用户提到以下关键词或场景时，**自动激活 Java 审计 skills**：
- 关键词：`审计`、`安全审计`、`代码审计`、`漏洞扫描`、`安全检测`
- 场景：分析 Java 项目的安全性、查找漏洞、检查 SQL 注入/文件上传/反序列化等

**激活后的行为**：
1. 根据用户需求选择合适的 skill 调用
2. 完整审计流程使用 `/java-audit-pipeline <项目路径>`
3. 单项审计按需调用：
   - 路由分析：`/java-route-mapper`
   - 调用链追踪：`/java-route-tracer`
   - SQL 注入审计：`/java-sql-audit`
   - 鉴权审计：`/java-auth-audit`
   - 文件上传审计：`/java-file-upload-audit`
   - 文件读取审计：`/java-file-read-audit`
   - XXE 审计：`/java-xxe-audit`
   - 反序列化审计：`/java-deserialization-audit`
   - 组件漏洞检测：`/java-vuln-scanner`

**使用前提**：
- 需要提供 Java 项目路径（源码或编译文件）
- 完整审计需开启 Agent Teams（Claude Code >= 2.1.32）

## 1. 想清楚再写
不要瞎猜，不要藏着困惑，把权衡讲出来。
-把你的假设明确说出来。不确定就问我。
-如果有多种理解，都摆出来，别自己悄悄选一个。
-如果有更简单的做法，直说。该反对的时候反对。
-如果哪里不清楚，停下来，说清楚卡在哪，然后问。

## 2. 简单优先
用最少的东西把问题解决，不做任何多余的事。
-不加我没要求的功能。
-不给一次性的活儿搭一套通用框架。
-不加我没要求的「灵活性」或「可配置」。
-不为不可能发生的情况提前操心。
-如果你交付的东西明显比需要的多，砍到刚好够用，重来。
-问自己一句：一个资深的人会不会觉得这过度复杂了？会的话，就简化。

## 3. 外科手术式改动
只动你必须动的，只收拾你自己制造的乱。
-不要去「改进」旁边没让你碰的内容、格式。
-不要翻新没坏的东西。
-跟着原本的风格走，哪怕你自己会用别的写法。
-看到无关的、原本就有的多余内容，提一句就行，别删。
-只收拾你这次改动产生的多余东西；原本就有的旧内容，不让你删就别删。
-一条判据：每一处改动，都要能直接追溯到我的需求。

## 4. 目标驱动执行
先定清楚「做到什么算成功」，然后对着标准跑到达标。
-「把这份资料整理好」→「按这三个维度分类（是什么、做什么、怎么做），每条梳理内容配一句出处」。
-（写代码的话）「加个校验」→「先写针对非法输入的测试，再让它们通过」。
-（写代码的话）「修复这个 bug」→「先写一个能复现 bug 的测试，再让它通过」。
-多步任务，先说一个简短计划，每一步对应一个验证点。
-成功标准给得够强，你才能自己对答案；标准太虚（比如「弄好就行」），就只能不停来问我。

---

<!-- SKILL-RULES-START -->
# Matt Pocock Skills 集成规则

> **标记**：`<!-- SKILL-RULES-START -->` 到 `<!-- SKILL-RULES-END -->` 之间为技能规则区域
> **版本**：2026-06-22
> **来源**：`npx skills@latest add mattpocock/skills`
> **技能目录**：`~/.agents/skills/`
> **修改说明**：如需修改技能规则，直接编辑此区域即可

## 技能概述

已安装 Matt Pocock 的工程技能包（34个技能），涵盖开发全流程。

## 核心技能使用指南

### 开发流程

#### 测试驱动开发 (/tdd)
- **触发场景**：实现新功能、修复 bug、需要测试先行时
- **核心原则**：测试行为而非实现，通过公共接口验证
- **工作流**：垂直切片，一次一个测试 → 实现 → 循环
- **禁止**：水平切片（先写所有测试再写所有实现）

#### 功能实现 (/implement)
- **触发场景**：基于 PRD 或 issues 实现功能
- **流程**：使用 /tdd → 定期类型检查 → 单文件测试 → 最终全量测试 → /review → 提交

#### 原型开发 (/prototype)
- **触发场景**：验证设计假设、探索 UI/逻辑方案
- **类型**：
  - 逻辑原型：验证状态/业务逻辑（终端应用）
  - UI 原型：多个 UI 变体，单路由切换
- **规则**：可丢弃、无持久化、无测试、完成后删除或吸收

#### Bug 诊断 (/diagnosing-bugs)
- **触发场景**：用户报告 bug、性能回归、系统异常
- **六阶段流程**：
  1. 构建反馈循环（核心技能）
  2. 复现 + 最小化
  3. 假设生成（3-5个，带预测）
  4. 插桩验证
  5. 修复 + 回归测试
  6. 清理 + 复盘

### 设计与架构

#### 代码库设计 (/codebase-design)
- **核心词汇**：Module, Interface, Depth, Seam, Adapter, Leverage, Locality
- **原则**：深度模块 = 小接口 + 大量实现
- **使用场景**：设计/重构模块接口时必须使用此词汇表

#### 领域建模 (/domain-modeling)
- **触发场景**：需要明确领域术语、记录架构决策
- **产出**：`CONTEXT.md`（术语表）+ `docs/adr/`（架构决策记录）
- **规则**：术语冲突时立即指出，模糊语言时提出精确术语

#### 架构改进 (/improve-codebase-architecture)
- **触发场景**：扫描代码库改进机会
- **产出**：HTML 报告（可视化 before/after）→ 选择候选 → /grilling 深入

### 项目管理

#### 问题分类 (/triage)
- **状态机**：needs-triage → needs-info / ready-for-agent / ready-for-human / wontfix
- **规则**：所有 AI 生成的评论必须以 `> *This was generated by AI during triage.*` 开头

#### PRD 编写 (/to-prd)
- **触发场景**：将对话转化为 PRD
- **产出**：问题陈述 + 解决方案 + 用户故事 + 实现决策 + 测试决策 + 范围外

#### Issue 分解 (/to-issues)
- **触发场景**：将计划分解为独立可抓取的 issues
- **原则**：垂直切片（tracer bullets），每个 issue 端到端完整

#### QA 会话 (/qa)
- **触发场景**：用户报告 bug、进行 QA、对话式文件 issues
- **流程**：监听 → 后台探索代码库 → 评估范围 → 创建 issue → 继续

### 审查与验证

#### 双轴审查 (/review)
- **触发场景**：审查分支、PR、工作进展
- **两轴**：
  - Standards：代码是否符合项目编码标准
  - Spec：代码是否匹配原始 issue/PRD
- **规则**：两轴并行运行，分别报告，不混合

#### 深度访谈 (/grilling)
- **触发场景**：压力测试计划、设计
- **方式**：一次一个问题，逐个解决设计树分支

### 协作与交接

#### 任务交接 (/handoff)
- **触发场景**：压缩当前对话供其他 agent 接手
- **产出**：交接文档（保存到临时目录）+ 建议技能列表

#### 教学 (/teach)
- **触发场景**：用户想学习新技能或概念
- **产出**：MISSION.md + 参考资料 + 学习记录 + 课程 + 笔记

### 配置与设置

#### 初始设置 (/setup-matt-pocock-skills)
- **触发场景**：首次使用技能前配置项目
- **配置项**：
  - Issue tracker（GitHub/GitLab/本地 markdown/其他）
  - Triage labels（5个标准角色的标签映射）
  - Domain docs（单上下文/多上下文）

#### Git 安全护栏 (/git-guardrails-claude-code)
- **触发场景**：防止危险 git 操作
- **阻止**：git push、git reset --hard、git clean -f、git branch -D 等

## 工作流集成

### 典型开发流程

1. **需求阶段**：/grilling → /domain-modeling → /to-prd
2. **规划阶段**：/to-issues → /decision-mapping（如需多会话）
3. **实现阶段**：/implement（内含 /tdd）
4. **审查阶段**：/review
5. **交接阶段**：/handoff

### Bug 修复流程

1. **诊断**：/diagnosing-bugs
2. **修复**：遵循诊断技能的六阶段
3. **验证**：回归测试 + /review

### 架构改进流程

1. **扫描**：/improve-codebase-architecture
2. **选择**：查看 HTML 报告
3. **深入**：/grilling + /domain-modeling
4. **实现**：/implement

## 注意事项

1. **CONTEXT.md 维护**：领域建模技能会主动更新，确保术语一致
2. **ADR 记录**：重要架构决策需记录到 `docs/adr/`
3. **Issue Tracker 配置**：首次使用需运行 /setup-matt-pocock-skills
4. **技能组合**：技能可串联使用，如 /triage 会调用 /grilling 和 /domain-modeling
5. **用户确认**：关键决策点需等待用户确认后再继续

## 可用技能列表

### Mattpocock Skills (17个)
- /ask-matt - 向 Matt 提问
- /codebase-design - 代码库设计
- /diagnosing-bugs - Bug 诊断
- /domain-modeling - 领域建模
- /grill-with-docs - 文档深度审查
- /improve-codebase-architecture - 改进代码库架构
- /prototype - 原型开发
- /setup-matt-pocock-skills - 设置技能配置
- /tdd - 测试驱动开发
- /to-issues - 转换为 Issues
- /to-prd - 转换为 PRD
- /triage - 问题分类
- /grill-me - 深度提问
- /grilling - 深度审查
- /handoff - 任务交接
- /teach - 教学
- /writing-great-skills - 编写优质技能

### General (17个)
- /design-an-interface - 接口设计
- /qa - 质量保证
- /request-refactor-plan - 重构计划请求
- /ubiquitous-language - 统一语言
- /implement - 实现
- /resolving-merge-conflicts - 解决合并冲突
- /decision-mapping - 决策映射
- /review - 代码审查
- /writing-beats - 写作节拍
- /writing-fragments - 写作片段
- /writing-shape - 写作结构
- /git-guardrails-claude-code - Git 安全护栏
- /migrate-to-shoehorn - 迁移到 Shoehorn
- /scaffold-exercises - 练习脚手架
- /setup-pre-commit - 设置 pre-commit
- /edit-article - 编辑文章
- /obsidian-vault - Obsidian 知识库
<!-- SKILL-RULES-END -->
